# Copia este archivo a .env.local para tests locales
# Conecta a PostgreSQL local (ej: docker compose up postgres -d)

PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=vania_dev
DB_PASSWORD=vania_dev
DB_NAME=vania_dev
DB_SCHEMA=public
JWT_SECRET=your_jwt_secret_key
ENV=dev

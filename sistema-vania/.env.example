# URL de la API (en producción pon la URL de tu API desplegada, ej. https://api-vania.onrender.com)
VITE_API_URL=http://localhost:3000

# Solo para desarrollo local con bypass de login (no uses en producción)
# VITE_ENV=dev
